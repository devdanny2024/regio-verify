# Server
PORT=3000

# Admin panel credentials (triggers browser Basic Auth dialog)
ADMIN_USER=admin
ADMIN_PASS=changeme

# PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/regio_verify

# Matrix homeserver (Synapse) — confirmed by Markus
MATRIX_HOMESERVER=https://matrix.151.hu

# Bot user — proj-admin1 on matrix.151.hu (admin of the Immo-Ungarn space)
MATRIX_BOT_TOKEN=syt_cHJvai1hZG1pbjE_mhcjgQfTfKvOFoEICRSO_1WMgVR
MATRIX_BOT_USER_ID=@proj-admin1:151.hu

# Markus's admin account — invited into every verification room automatically
MATRIX_ADMIN_USER_ID=@mm:151.hu

# Registration token — 200-use token provided by the matrix.151.hu homeserver admin
MATRIX_REGISTRATION_TOKEN=BPyERT2S~L7n8h5g

# Public base URL — used to build invite links shown in admin panel
BASE_URL=https://regio.is

# SMTP — local Postfix smart relay (set up by Andy on the VPS)
# Postfix forwards to the 151.hu mail server at Versanus — no auth required
SMTP_HOST=127.0.0.1
SMTP_PORT=25
SMTP_SECURE=false
SMTP_FROM=REGIO Verify <noreply@regio.is>
